#include<stdio.h>
#include<conio.h>
void main()
{
int i,j,a[20],n,temp,search,count;
clrscr();
printf("enter the size");
scanf("%d",&n);
printf("enter the elements");
for(i=0;i<n;i++)
{
scanf("%d",&a[i]);
}
printf("enter a number for search");
scanf("%d",&search);
for(i=0;i<n;i++)
{
if(a[i]==search)
{
printf("\n\t%d is present at location %d",search,i+1);
count++;
}
}
if(count==0)
{
printf("\n\t %d is not present in array", search);
}
else
{
printf("\n\t%d is present %d times in array",search);
}
getch();
}

