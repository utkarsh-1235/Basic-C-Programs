#include<stdio.h>
#include<conio.h>
void main()
{
int i,j,a[100],n,max,min;
clrscr();
printf("\t enter the size of an array \n");
scanf("%d",&n);
for(i=0;i<n;i++)
{
scanf("%d",&a[i]);
}
max=a[1];
min=a[1];
for(i=0;i<n;i++)
{
if(a[i]>max)
{
max=a[i];
}
else if(a[i]<min)
{
min=a[i];
}
}
printf("\t maximum element is %d \n",max);
printf("\t minimum element is %d \n",min);
getch();
}